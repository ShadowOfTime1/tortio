# Changelog

Все значимые изменения по версиям. Формат — обратный хронологический.

## v0.1.3 — 2026-04-30 (strip media permissions from play flavor)

После v0.1.2 Play всё ещё ругался — `open_filex` тянул за собой `READ_MEDIA_IMAGES/VIDEO/AUDIO` + `READ_EXTERNAL_STORAGE`, что Google требует декларировать как «доступ к фото/видео». Эти разрешения нужны только для open_filex (а он используется только в auto-update GitHub Releases — в play-flavor он не вызывается).

### Что изменилось
- Создан `android/app/src/play/AndroidManifest.xml` с `tools:node="remove"` для четырёх media-разрешений.
- В play-сборке остаётся только `INTERNET` — никаких декларируемых медиа-разрешений.
- github-flavor не затронут — там open_filex продолжает работать.

## v0.1.2 — 2026-04-30 (build flavors: play vs github)

Google Play отклонял v0.1.1 в Closed Testing из-за `REQUEST_INSTALL_PACKAGES` — это разрешение нужно нашему auto-update механизму для GitHub Releases, но для Play-сборки оно лишнее (Play обновляет приложение сам) и Google требует отдельной декларации с обоснованием.

### Что изменилось
- **Build flavors `play` и `github`** в `android/app/build.gradle.kts` (dimension `distribution`).
- **`REQUEST_INSTALL_PACKAGES` убрано** из `android/app/src/main/AndroidManifest.xml`, перенесено в новый `android/app/src/github/AndroidManifest.xml` (только github-flavor его получает).
- **`UpdateService.checkForUpdate()`** возвращает `null` в play-сборке — баннер обновлений показывается только в github-сборке. Контролируется через `--dart-define=DISTRIBUTION=play|github`.
- **`.github/workflows/release.yml`** теперь собирает оба варианта:
  - `app-github-release.apk` (с auto-update) для GitHub Releases пользователей
  - `app-play-release.aab` (без install permission) для Play Console

### Что НЕ меняется
- Auto-update для GitHub Releases пользователей продолжает работать как раньше.
- Play Store пользователи получают обновления через Play автоматически.

## v0.1.1 — 2026-04-29 (custom app icon + Play listing assets)

Подготовка финальной версии для Google Play. Старая дефолтная Flutter-иконка заменена на собственную брендовую.

### Что добавилось
- **Новая иконка приложения** — pink/orange gradient (#FF6B8A → #FF8E53, как в UI приложения), белый трёхъярусный торт с frosting drip и свечкой. Заменяет дефолтный Flutter-логотип во всех mipmap-плотностях (mdpi/hdpi/xhdpi/xxhdpi/xxxhdpi). Сгенерирована Pillow-скриптом `tools/make_icon.py` (1024×1024 master + 5 размеров для Android + 512×512 для Play).
- **Play Store assets** в `docs/play/`:
  - `icon-512.png` — иконка для Play (512×512).
  - `icon-1024-master.png` — мастер-копия (для будущих ребрендов).
  - `feature-graphic-1024x500.png` — feature graphic для Play (брендированный баннер с названием и tagline).
  - `screenshots-en/` — 6 скриншотов на английском (welcome, empty, list, wedding scaler, chocolate scaler, ingredients).
  - `screenshots-ru/` — 5 скриншотов на русском (без welcome — system locale на эмуляторе EN).
  - `PLAY_LISTING.md` — гайд по заполнению Play Console: тексты описаний (короткое 80 ch + полное ~1700 ch на каждый язык), категория, content rating, data safety, release track recommendations.

### Дальше
v0.1.1 AAB пойдёт в Play Console (после получения Developer Account verification от Google).

## v0.1.0 — 2026-04-28 (package rename + Play Store signing)

**Breaking change для существующих установок**: новый applicationId означает, что Android считает это **другим приложением**. Старая `com.example.tortio` НЕ может быть автоматически обновлена до `com.tortio.app` — это невозможно технически. Существующие тестовые установки нужно вручную удалить и переустановить новую версию.

### Что изменилось
- **Android applicationId / namespace**: `com.example.tortio` → `com.tortio.app` в `android/app/build.gradle.kts`.
- **MainActivity.kt** перемещён `kotlin/com/example/tortio/` → `kotlin/com/tortio/app/`, package declaration обновлён.
- **iOS / macOS / Linux bundle IDs** — обновлены для консистентности (для будущих платформ).
- **Keystore infrastructure**: добавлена поддержка release-signing через `key.properties` (build.gradle.kts уже её ожидал, но файла не было). `key.properties`, `*.jks`, `*.keystore` добавлены в `.gitignore`.
- **`docs/SIGNING.md`** — гайд для maintainer'а: создание keystore, бэкапы, signed APK/AAB сборка, опциональная интеграция в GitHub Actions.

### Что осталось до Play-релиза
- [ ] Создать release keystore (требует пароля от user'а) и `key.properties`.
- [ ] Сборка signed AAB локально (`flutter build appbundle --release`).
- [ ] Проверка signed APK/AAB на эмуляторе.
- [ ] Решение по GitHub Actions: добавить keystore в secrets для автоподписи в CI или оставить ручную локальную сборку для Play.
- [ ] Listing assets (icon 512×512, feature graphic 1024×500, screenshots EN+RU, описания).
- [ ] Регистрация Google Play Developer Account.
- [ ] Internal testing track → Closed → Production.

## v0.0.16 — 2026-04-28 (Privacy Policy + GitHub Pages site)

Подготовка к Google Play: поднят публичный сайт-документация и in-app ссылка на Privacy Policy.

### Что добавилось
- **GitHub Pages site** на `https://shadowoftime1.github.io/tortio/` из папки `docs/` ветки `main`. Включает:
  - `index.md` — landing page с описанием приложения и ссылками.
  - `privacy-policy.md` — Privacy Policy (English).
  - `privacy-policy-ru.md` — Политика конфиденциальности (Russian).
- **Privacy Policy** покрывает: что приложение НЕ собирает (всё локально), Google Drive scope (`drive.appdata` only), GitHub Releases для апдейтов, отсутствие аналитики/рекламы/трекинга, права пользователя, контакты.
- **In-app ссылка**: Settings → About → Privacy policy. Открывает русскую или английскую версию в зависимости от текущей локали приложения.

### Verified
- Оба URL `/privacy-policy/` и `/privacy-policy-ru/` отдают HTTP 200.
- Эмулятор: тап на ListTile в settings открывает Chrome с правильным URL.

## v0.0.15 — 2026-04-28 (3 sample recipes + multi-tier section count fix)

Богаче onboarding для новых юзеров — вместо одного sample-рецепта создаются 3 разной сложности, чтобы можно было сразу пощупать одноярусные, многоярусные и разные scale-types.

### Что добавилось
- **Светлый бисквит (sample)** — самый простой, 18 cm, 2 секции (бисквит + крем). Для новичков, демонстрирует базовый flow.
- **Шоколадный торт (sample)** — средняя сложность, 22 cm, 3 секции (бисквит + крем + глазурь). Уже был, остался как есть.
- **Свадебный торт (sample)** — двухъярусный, 26 cm + 16 cm, демонстрирует multi-tier scaler и фиксированный декор (sugar figures, scale=fixed).
- Кнопка пустого экрана: «Create sample recipe» → «Create sample recipes» (plural).
- 17 новых ARB-ключей для нового контента.

### Bug fix
- **Multi-tier карточка показывала неправильное число секций**: `r.sections.length` считал только root tier. Теперь `r.allTiers.expand((t) => t.sections).length` — все секции из всех ярусов. Wedding cake теперь корректно показывает «5 sect.» вместо «2 sect.».

### Verified на эмуляторе
- Wipe data → welcome → Get started → Create sample recipes → 3 рецепта в списке.
- Wedding cake → scaler: «2 tier(s) • total 2,4 kg», Tier 1 (26×8 cm), Tier 2: Top (16×8 cm) с decor section, все ингредиенты и units английские.

## v0.0.14 — 2026-04-28 (PDF/share-text localization + Ø glyph fix)

Закрываем последний UI-русизм: PDF-экспорт и share-text. Теперь EN-юзер делится и сохраняет рецепты в полностью английском формате.

### Что добавилось
- **PDF-экспорт локализован**: `PdfExportService.exportScaledRecipe(... required AppLocalizations l)`. Все строки идут через ARB:
  - Subtitle (`pdf_subtitle_multitier` / `pdf_subtitle_size_h` / `pdf_subtitle_size`)
  - Tier label (`scaler_tier_label` / `scaler_tier_label_named`)
  - Tier summary (`pdf_tier_summary_h` / `pdf_tier_summary`)
  - Section header через `displayName(l)` (был хардкод `s.type.name`)
  - Units (`unit_grams_short` / `unit_kilograms_short` / `unit_pieces_short` / `unit_centimeters_short`)
- **Share-text локализован** в `_shareRecipe`: dimensions через `share_diameter`/`share_height`/`share_total_approx`, section names через `displayName(l)`, units через локализованные suffix'ы, `share_notes_header` вместо хардкода «📝 Заметки».
- **Ø glyph fix в PDF**: символ `⌀` (U+2300) отсутствует в Roboto и рендерился квадратиком. Заменяем PDF-локально на `Ø` (U+00D8) через `_pdfSafe()` helper. UI остаётся с `⌀` — системные шрифты на Android его рендерят корректно.

### Verified на эмуляторе
- Share as text: 🍰 Chocolate cake (sample) / Ø 22 cm • height 8 cm • ~1,5 kg / Sponge → Flour 200 g, ... / 📝 Notes / ... — всё английское.
- PDF: title «Chocolate cake (sample)», subtitle «Ø 22×8 cm • total ≈ 1,5 kg», tier label «Tier 1 • Ø 22×8 cm • 1,5 kg», section headers Sponge/Cream/Glaze, ингредиенты, units g/kg/cm, footer «Tortio • 28.04.2026». Глиф Ø рендерится корректно.

### Локализация — полностью закрыта
Теперь во всех output-форматах (UI, PDF, share-text) контент идёт в выбранной локали. Из «русского» в EN-локали остаются только: имена custom-типов и user-input ингредиентов (это user data) и labels пользовательских secций/тегов.

## v0.0.13 — 2026-04-27 (sample recipe localization)

Закрываем последний UI-видимый русский кусок при чистой установке в EN локали.

### Что добавилось
- **Sample recipe генерируется в текущей локали**: `buildSampleRecipe(AppLocalizations l)` вместо хардкода. 13 новых ARB-ключей: `sample_title`, `sample_notes`, `sample_sponge_notes`, `sample_tag_chocolate`, `sample_tag_sample`, `sample_ing_flour/cocoa/sugar/eggs/butter/cream33/powdered_sugar/dark_chocolate`.
- **EN-юзер при первом запуске** видит «Chocolate cake (sample)», теги «chocolate»/«sample», ингредиенты Flour/Cocoa/Sugar/Eggs/Butter/Cream 33%/Powdered sugar/Dark chocolate, английские notes. Русский юзер — то же самое на русском.
- **Поведение**: содержимое фиксируется в момент создания sample, после смены локали уже не пересоздаётся (это user data).

### Verified на эмуляторе
Чистая установка → welcome (English) → Get started → Create sample recipe → карточка «Chocolate cake (sample) ⌀ 22 cm • 3 sect. • 9 ingr.» → scaler показывает Sponge/Cream/Glaze со всеми английскими ингредиентами и notes на английском.

### Что осталось намеренно на русском
- PDF/share-text export — output для печати/отправки.
- Имена custom-типов и ингредиентов пользователя — user input.

## v0.0.12 — 2026-04-27 (английская локализация — фаза 3: preset section names)

Закрываем последний крупный гэп локализации, найденный при пост-релизной верификации v0.0.11.

### Что добавилось
- **Preset section names локализованы**: 10 ARB-ключей (`preset_sponge` / `preset_cream` / `preset_filling` / `preset_coating` / `preset_ganache` / `preset_syrup` / `preset_mousse` / `preset_meringue` / `preset_glaze` / `preset_decor`). Метод `SectionType.displayName(l)` возвращает локализованное имя для пресета (узнаётся по канонической русской строке `name`) или сам `name` как есть для custom-типов. Storage-формат не меняется — backward compatible.
- **Scale labels локализованы**: новые ARB-ключи `scale_label_volume` / `scale_label_area` / `scale_label_fixed` + метод `SectionType.scaleLabelLocalized(l)`. Используется в form section card subtitle, scaler section header badge, settings → custom types subtitle (раньше там вообще английский enum.name "volume" просачивался даже в RU локали — заодно фикс багу).
- **Display sites переключены на `displayName(l)`**: scaler tier card section header (single + multi), shopping list bottom sheet, add-recipe form section card, section picker chip, custom-type actions dialog title.
- **«3 sec.» → «3 sect.»** в карточке рецепта — снимаем двусмысленность (мог читаться как seconds).

### Verified на эмуляторе
Section picker полностью английский: Sponge / Cream / Filling / Coating / Ganache / Syrup / Mousse / Meringue / Glaze / Decor. Scaler tier card показывает «🍰 Sponge | volume», «🍦 Cream | volume», «✨ Glaze | area». Карточка списка — «3 sect. • 9 ingr.».

### Что осталось (намеренно)
- Sample recipe content (Шоколадный торт пример + ингредиенты + notes) — данные, не UI.
- PDF/share-text — output для печати/отправки.
- Имена custom-типов и ингредиентов пользователя — user-input.

## v0.0.11 — 2026-04-27 (английская локализация — фаза 2: формы и пересчёт)

Доделка локализации после самопроверки в эмуляторе. Теперь все 4 основных экрана полностью на английском при выбранном языке English.

### Что добавилось в этом релизе
- **Форма создания/редактирования рецепта** — целиком: name / size / extras (notes / rating / tags) / composition / add tier / unsaved-dialog / photo picker / validation errors.
- **Экран пересчёта (scaler)** — полностью: tabs by size/by weight, slider labels, original size/weight, total weight, height label, "I cooked it" snackbar, export menu, shopping list bottom sheet.
- **Settings — оставшиеся экраны:** custom types (empty state + add), quick diameters + helper text + default mode radios, auto-update toggle + subtitle + check-now, export/import/undo, stats sheet, danger zone (reset settings + delete all + confirmation dialogs), about (version, welcome again, GitHub source, license).
- **Section picker dialog**: «Choose section», категории (Base / Creams & fillings / Coatings & syrups / Decor), «Create custom type».
- **Custom section type dialog** (создание/редактирование) с полями name / icon / scale type радиогруппой.
- **Localized units everywhere**: `formatGrams` и `formatAmount` теперь принимают unit-suffix параметры (g/kg/pcs vs г/кг/шт). Пересчёт, scaler-итоги, ингредиент-amounts, shopping list — всё показывает в выбранном языке.
- **`_UnitToggle`** в форме рецепта показывает локализованный label, при этом storage-id ('г'/'шт') остаётся стабильным — старые рецепты не ломаются.

### Что НЕ переведено (намеренно — data, не UI)
- **Названия preset-секций** («Бисквит», «Крем», «Мусс», «Декор» и т.д.) — это data в `SectionType.presets`. Чтобы локализовать, нужна миграция: ввести stable internal IDs ('sponge', 'cream', ...), хранить их в storage, отображать через ARB. Существующие рецепты ссылаются на эти имена — простой rename сломает их данные.
- **Пресет-категории scale-types** ("объём" / "площадь" / "фикс") — приходят через `section.type.scaleType.name` (Dart enum name). Нужен helper.
- **Sample recipe** ("Шоколадный торт (пример)" + ingredient names + notes) — это demo-data, генерируется sample_recipe.dart на русском.
- **PDF export** и **share-text** оставлены на русском — это output для отправки/печати, не in-app UI.
- **Имена ингредиентов** в существующих рецептах — пользовательские данные, не подлежат автопереводу.

После Phase 2 пользователь с выбранным English видит весь UI на английском, включая формы, диалоги и юнит-суффиксы. Остаётся только data (названия пресет-секций и contents sample-recipe).

## v0.0.10 — 2026-04-27 (английская локализация — фаза 1)

Первая фаза i18n. Инфраструктура + основные экраны переведены на английский. Глубокие экраны (форма рецепта, экран пересчёта) пока на русском — добью в следующем релизе.

### Инфраструктура
- Подключены `flutter_localizations` + `intl` + `gen-l10n`. ARB-файлы лежат в `lib/l10n/app_ru.arb` и `lib/l10n/app_en.arb`. Класс `AppLocalizations` генерируется на pub get.
- **Переключатель языка в Settings → «Язык»**: Системный / Русский / English. Дефолт — системный (если язык устройства русский → русский UI; иначе английский). Выбор сохраняется в SharedPreferences через `LocaleService`.
- `LocaleService` — синглтон-`ChangeNotifier`, `MaterialApp` пересобирается при смене языка без перезапуска.

### Что переведено в этом релизе
- **Welcome-диалог** (приветствие при первом запуске).
- **Главный экран — список рецептов**: заголовок, поиск, фильтр тегов, FAB «+ Recipe», empty state, sort-меню, popup-меню рецепта (Pin/Edit/Duplicate/Select/Delete), bulk-удаление с диалогом, undo-snackbar, NEW-бейдж, инфостроки карточек («cooked N times today»).
- **Settings**: заголовки групп (Appearance, Backup, Cloud, Danger zone, About), переключатель темы, новый блок «Cloud backup» (Google Drive подключение/отключение/sync/restore с диалогами).
- **Update-баннер** на главном экране.
- **AppBar** формы рецепта и диалог «Unsaved changes».
- **AppBar** экрана пересчёта (tooltips для shopping list / I cooked it).

### Что НЕ переведено (Phase 2)
- Тело формы рецепта (поля, секции, ингредиенты, ярусы, фото-пикер, ошибки валидации, ~135 строк).
- Тело экрана пересчёта (слайдеры, экспорт-меню, список покупок, snackbars, ~74 строки).
- Глубокие диалоги Settings: кастомные типы, quick-диаметры, импорт/экспорт, опасная зона, About.
- Ошибки сервисов (image_picker, update_service, drive_backup_service).
- Sample-рецепт (демо-кнопка создаёт «Шоколадный торт» — это пока русский title и состав в любой локали).

Если переключить язык на English — UI смешанный: главный экран и settings-шапки на английском, форма редактирования и пересчёт всё ещё на русском. Это намеренный частичный шип — функционал не сломан, переводы добавляются итеративно.

## v0.0.9 — 2026-04-27 (облачный бэкап в Google Drive)

Главный риск для пользователя — потеря телефона и всей коллекции рецептов. Локальный экспорт в JSON есть с v1.10.0, но он требует ручного действия и про него легко забыть. Теперь — автоматический бэкап в Google Drive.

### Что появилось
- **Settings → «Облачный бэкап»** — новая группа над «Резервные копии».
  - До входа: «Подключить Google Drive» с подзаголовком «Автоматический бэкап рецептов».
  - После входа: email аккаунта, время последней синхронизации, кнопки «Синхронизировать сейчас», «Восстановить из облака», «Выйти».
- **Автосинхронизация после каждого сохранения рецепта.** Debounce 20 сек — несколько правок подряд схлопываются в один upload, экономия трафика.
- **Восстановление при подключении.** Если на Drive уже есть бэкап (например, переустановили приложение), при логине предлагается «Восстановить из бэкапа от такой-то даты?». Тап — рецепты заменяются содержимым бэкапа. Иначе — текущая коллекция уезжает в облако.
- **Тихая авторизация при старте.** Если пользователь раньше входил — `signInSilently` подцепляет аккаунт без UI (баннеров не будет).

### Технические детали
- Хранилище — `appDataFolder` (скрытая папка приложения в Drive). Не видна пользователю в UI Drive, не требует Google verification, не светит данные сторонним приложениям.
- Один файл `recipes.json` на пользователя, формат идентичен ручному JSON-экспорту.
- Конфликтов нет — last-write-wins по `modifiedTime`. На одном устройстве у одного пользователя реальных конкурирующих записей не бывает.
- Если пользователь не подключал Google Drive — приложение работает как раньше, никаких сетевых вызовов.
- Если интернет пропал во время автосинка — ошибка проглатывается тихо, локально всё сохранено, следующий save попробует снова.

### Что НЕ сделано (намеренно)
- Картинки рецептов в облако не уходят — только JSON. Картинки слишком тяжёлые, требуют отдельной стратегии (рассмотрим позже, если будет запрос).
- Нет push-уведомлений «бэкап давно не синкался» — пока не требуется, синк автоматический.

## v0.0.8 — 2026-04-26 (большой пакет фиксов из аудита)

Самостоятельный аудит на эмуляторе вскрыл 11 проблем — все починены в одном релизе.

### Критика
- **Undo при удалении не работал.** Кнопка «Отменить» в snackbar пересекала зону FAB «+ Рецепт» — тап перехватывался FAB, открывалась форма нового рецепта, snackbar пропадал, удаление становилось необратимым. Поднял snackbar margin'ом 88dp над FAB; duration увеличил до 6 сек.

### Потеря данных
- **Несохранённые изменения теперь спрашиваются при выходе.** В форме редактирования: PopScope + автоматическое отслеживание dirty (listeners на все controllers + пометка в setState'ах). При попытке выйти с несохранёнными изменениями — диалог «Остаться / Выйти».

### Читаемость
- **Имена ингредиентов больше не обрезаются.** Ratio name:amount изменён с 3:2 на 5:3, плашка `г` уменьшена (padding 6/3, fontSize 11), крестик удаления переведён с IconButton на компактный InkWell. «Сливочное масло», «Сахарная пудра» теперь видны.
- **Имя рецепта в карточке.** NEW + рейтинг вынесены на вторую строку рядом с «⌀ X см • N секц.». Имя получило всю ширину. Звёзды показываются компактно как «★ N» только если рейтинг > 0.
- **Placeholder яруса.** Поле «Название (опц)» вынесено в отдельную строку под бейджем «Ярус N» — больше не обрезается.

### Косметика и консистентность
- **FAB «+ Рецепт» в бренд-цвет** (#FF6B8A вместо дефолтного фиолетового).
- **Иконки status bar читаемы во всех экранах.** Добавил `appBarTheme.systemOverlayStyle` в обеих темах — раньше Material 3 AppBar переопределял глобальный AnnotatedRegion и в светлой теме иконки часов/wifi становились невидимыми.
- **Иконка слайдера высоты** (Icons.height ≡) заменена на текстовый лейбл «В» / «Высота» — не путается с drag handle.
- **Settings: «Опасная зона» и «О приложении»** разделены divider'ом.
- **Suffix `см` у пустой Высоты** теперь виден (`floatingLabelBehavior: always`) — раньше Material скрывал suffix у unfocused empty полей, поля Диаметр/Высота выглядели по-разному.

## v0.0.7 — 2026-04-26 (закрытие SnackBar удаления)

- **Плашка «X удалён • Отменить» висела бесконечно у пользователя.** Корневая причина — Android Accessibility Time-to-take-action: эта системная настройка умножает `duration` любого SnackBar (вплоть до «без ограничения»). У стандартного `SnackBar` нет явной кнопки закрытия — только свайп вниз, о котором пользователь не знает. Заменил `action:` на кастомный `content` с двумя кнопками: «Отменить» и иконка ✕. Теперь плашку можно закрыть одним тапом независимо от системного таймера.

## v0.0.6 — 2026-04-26 (дедуп в списке покупок)

- **Список покупок: один и тот же ингредиент мог попадать дважды.** Агрегация работала по `name.trim().toLowerCase()` и единице, но если в одной секции пользователь набирал `Сахар`, в другой `Сахар.` или `Сахар  тростниковый` (двойной пробел внутри) — это были разные ключи и они не сливались. Добавил `_normalizeNameKey`: схлопывает любые внутренние пробелы в один и стрипает завершающие `.`/`,`/`;`. Отображаемое имя берётся из первого вхождения как раньше.

## v0.0.5 — 2026-04-26 (косметика формы редактирования)

- **Числа в полях больше не показывают `.0`.** В форме редактирования значения подгружались как сырой `'${value}'` от `double` — `250` отображалось как `250.0`, `16` как `16.0`. Теперь все числовые поля (диаметр, высота, вес, количество ингредиента) проходят через `formatNumber` — целые числа без хвоста, дробные с запятой.
- **Диаметр и Высота выровнены по верху.** `helperText: 'необязательно'` под полем Высоты делал его выше Диаметра, поля уезжали по вертикали из-за дефолтного `CrossAxisAlignment.center` в Row. Подсказку «(высота — опционально)» вынес в подзаголовок «Размеры формы», поля теперь одинакового размера и выровнены.

## v0.0.4 — 2026-04-26 (фикс перекрытия цифр)

- **Плашка `г`/`шт` накрывала последнюю цифру количества.** В v0.0.3 переключатель единиц был добавлен как `suffixIcon` поля ввода с `isDense: true`, и Flutter не резервировал под него место — `250` отображалось как `25(`, `40.0` как `40.`. Вынес переключатель из `InputDecoration` рядом с полем отдельным виджетом — теперь цифры не обрезаются, поле ввода занимает всё доступное место.

## v0.0.3 — 2026-04-23 (отзывы реального пользователя)

Три бага из теста на живом пользователе.

- **Высота торта стала необязательной.** Раньше форма не давала сохранить рецепт без высоты — но кондитеры часто не знают точную высоту изделия. Теперь поле «Высота» с пометкой «необязательно», и при пустом значении рецепт скейлится только по площади (диаметру). Действует и для основного торта, и для каждого яруса.
- **Единицы измерения у ингредиентов: г / шт.** Яйца, желатин в листах и подобные штучные ингредиенты раньше можно было ввести только в граммах. Теперь у каждого ингредиента есть переключатель `г` ⇄ `шт` справа в поле количества. Список покупок и итоги корректно агрегируют по `(имя, единица)` — «Яйца, шт» и «Яйца, г» считаются раздельно.
- **Высота меняется в скейлере.** Раньше можно было изменить только диаметр при пересчёте. Теперь под слайдером диаметра — слайдер высоты (если у рецепта/яруса она задана) с числовым полем рядом. Объёмные секции (бисквиты, муссы) корректно пересчитываются по двум осям одновременно.

## v0.0.2 — 2026-04-24 (фикс multi-tier)

3 связанных бага в форме редактирования рецепта.

- **Закрепление и история готовки слетали при сохранении.** `_save` строил новый `Recipe(...)` без передачи `cookCount`, `lastCookedAt`, `pinned` — они уходили в дефолтные 0/0/false. После любого «Сохранить» рецепт терял пин и счётчик готовки. Теперь явно прокидываются из `widget.existingRecipe`.
- **Новый ярус был «невидимым».** При тапе «+ Ярус» новая карточка добавлялась внизу формы, ниже видимой области. Пользователь не видел, что ярус добавился (с автозаполненными размерами), думал «не сработало», скроллил, перевводил значения вручную. Теперь после добавления `ScrollController` плавно прокручивает к новому ярусу.
- **Молчаливое отбрасывание пустых ярусов.** Если ярус 2+ имел пустые размеры или нулевые ингредиенты, он `continue;`-скипался без сообщения, рецепт сохранялся без него. Теперь показывается ошибка «Ярус(ы) X пропущены: нет ингредиентов с весом > 0», save не выполняется.

## v2.7.1 — 2026-04-24 (последняя в цикле 2.x)

Транзитный релиз перед сбросом версии до 0.0.1 + UX-полировка.

- **Закрепление рецепта.** В попап-меню (⋮) — «Закрепить»/«Открепить». Закреплённые висят сверху списка над любой сортировкой/фильтром, с иконкой 📌 у названия. Drag-reorder отключается, пока есть закреплённые (индексы visible vs `_recipes` расходятся).
- **«Показать приветствие снова»** в Settings → О приложении. Сбрасывает флаг `welcome_seen` и заново открывает welcome bottom sheet.
- **Логика auto-update переписана** — раньше сравнивали по semver (`2.7.0 > 2.0.0`). Теперь «любой отличающийся от установленной версии тег = обновление». Это нужно для предстоящего сброса версии до 0.0.1 (иначе `0.0.1 < 2.7.0` блокировало бы апгрейд). Метод переименован: `_isNewer` → `_shouldOfferUpdate`.

## v0.0.1 — 2026-04-24 (новая версионная схема)

Сброс версии до 0.0.1 по запросу. Фактически — то же приложение, что v2.7.1, но начинаем нумерацию заново.

## v2.7.0 — 2026-04-24

Дальше упрощаем для новичка.

- **Welcome-диалог при первом запуске.** Bottom sheet с кратким туром: что делает приложение, как добавить рецепт, как пересчитать, ярусные торты, иконки в шапке. Показывается один раз — флаг `welcome_seen` в SharedPreferences.
- **Группировка пресетов в пикере секций** по категориям:
  - **Основа:** Бисквит, Безе
  - **Кремы и начинки:** Крем, Начинка, Мусс
  - **Покрытия и пропитки:** Покрытие, Ганаш, Пропитка, Глазурь
  - **Декор:** Декор
  - **Свои типы:** кастомные (если добавлял)
  - Кнопка «Создать свой тип» отдельно внизу.

## v2.6.0 — 2026-04-24

UX-оптимизация: меньше «всего сразу», проще новичку.

- **Демо-рецепт на пустом экране.** Когда рецептов нет, под «Добавьте свой первый торт!» появилась кнопка **«Создать примерный рецепт»**. По тапу — добавляется готовый шоколадный торт с 3 секциями (бисквит/крем/глазурь), заметками, тегами и оценкой. Новичок сразу видит, что и куда вписывается.
- **«Дополнительно» сворачивается** в форме рецепта. Поля **вес / заметки / оценка / теги** теперь спрятаны под `ExpansionTile` — для нового рецепта свёрнуто, при редактировании рецепта с данными — раскрыто автоматически. Новичок видит только обязательные поля: название, размер, состав.
- **«Экспорт» меню** в шапке экрана пересчёта вместо двух отдельных иконок «PDF» и «Поделиться» — одна иконка `↗` с popup'ом «Поделиться текстом / Сохранить как PDF». Меньше иконок в шапке.

## v2.5.1 — 2026-04-24

- **Тёплый градиент фона** вместо плоской заливки. Светлая тема: кремовый → нежно-розовый по диагонали. Тёмная: кофейно-чёрный → тёмно-винный. Применяется на всех экранах через тот же `DotOrnament`.
- **Орнамент крупнее** — иконки 70–110px (раньше 40–60), позиций меньше (10), спред больше. Узор «дышит» декоративно вместо мелкой ряби.
- **Карточки рецептов непрозрачные** — alpha поднят с 0.3/0.2 до 0.85/0.7 (выбранный — 1.0/0.9). Карточки теперь явно отделяются от фона, не «растворяются».

## v2.5.0 — 2026-04-24

- **Кондитерский орнамент.** Точки заменил на рассеянные иконки cake / bakery / cookie / icecream / cafe (Material Outlined) при низкой прозрачности. Тематично, не режет глаза.
- **Орнамент на ВСЕХ экранах** — главный, форма рецепта, экран пересчёта (single-tier и multi-tier). Раньше его не было на экране пересчёта.
- **«Авто» тема теперь по времени суток** (06:00–20:00 — светлая, иначе тёмная). Раньше она следовала за системой, и если телефон был в dark mode круглые сутки, приложение тоже было тёмным днём. Теперь зависит от часа, не от системы. Подпись в Settings обновлена: «Авто (день — светлая, вечер — тёмная)».
- **Авто-заполнение нового яруса.** При нажатии «Добавить ярус» диаметр предзаполняется как (предыдущий ярус − 4 см, минимум 10), высота берётся как у предыдущего. Поля редактируемые. Стандартный паттерн ярусного торта (например 30→26→22 см).

## v2.4.2 — 2026-04-24

- **Орнамент теперь на главном экране тоже.** В v2.4.1 он был только в форме рецепта, поэтому в списке ничего не было видно. Вынес `DotOrnament` в общий виджет (`lib/widgets/dot_ornament.dart`), подключил и к recipe_list, и к add_recipe.
- **Иконки статус-бара видимы в светлой теме.** В предыдущих версиях системные иконки (часы, WiFi, батарея) рисовались светлым на светлом фоне → невидимы. Теперь через `AnnotatedRegion<SystemUiOverlayStyle>` контрастность подбирается автоматически: тёмные иконки в светлой теме, светлые в тёмной.

## v2.4.1 — 2026-04-24

- **Фон формы рецепта.** Убраны розово-оранжевые «пятна» из v1.16.0 (фон стал казаться «никаким»). Вместо них — точечный орнамент через `CustomPaint` (точки в шахматном порядке, как на пергаментной бумаге для выпечки). Тёмный режим использует светлые точки, светлый — пастельно-розовые.

## v2.4.0 — 2026-04-24

- **«Я приготовил»** — кнопка с иконкой ✓ в шапке экрана пересчёта (single-tier и multi-tier). Тап увеличивает счётчик, обновляет «последнее приготовление = сейчас», haptic feedback + розовый SnackBar «Записал! Готовите N-й раз».
- **Карточка списка** показывает «Готовили N раз · X дн. назад» если рецепт хоть раз готовили.
- **Две новые сортировки** в меню: «Чаще готовлю» (по `cookCount`) и «Недавно готовил» (по `lastCookedAt`).
- Дубликат рецепта стартует с обнулённой статистикой (это новый рецепт). Импортированные рецепты тоже без личной статистики (для backup-restore используйте экспорт/импорт через Settings — там сохраняется всё).
- Storage: оба поля сериализуются только если `> 0` — экономим место в JSON.

## v2.3.0 — 2026-04-24

- **Личный рейтинг рецептов** 0–5 звёзд. В форме редактирования — селектор звёзд (тап ставит N, повторный тап по той же звезде или кнопка «Снять» обнуляет). На карточке списка — звёзды у названия, если оценка ≥ 1.
- **Сортировка «По рейтингу»** в меню сортировки — высокие оценки первыми, без оценки в конце.
- Storage: новое поле `rating` сохраняется только если `> 0` (старые JSON грузятся как `0` = не оценено).

## v2.2.0 — 2026-04-24

- **PDF экспорт** «технологической карты». В шапке экрана пересчёта появилась кнопка с иконкой PDF. Тап → генерируется PDF с актуальными (масштабированными) количествами, открывается системный share-диалог.
- **Содержимое PDF:**
  - Заголовок: название рецепта (розовый), общая инфа (диаметр×высота или «N ярусов»), итоговый вес.
  - Теги внизу заголовка как маленькие чипы.
  - Заметки рецепта (если есть) в светло-розовой рамке.
  - **Каждый ярус** отдельным блоком: розовая полоса с меткой/размерами/весом, ниже секции таблицами «ингредиент | вес».
  - Per-section заметки курсивом под таблицей.
  - Подвал: «Tortio • DD.MM.YYYY».
- **Шрифт:** Roboto (Regular/Bold/Italic) грузится через `PdfGoogleFonts` — есть кириллица. Первый запуск требует интернет, дальше кешируется.
- **Имя файла:** `tortio-{title}-{YYYY-MM-DD}.pdf` (с очисткой от спецсимволов).

## v2.1.0 — 2026-04-24

- **Унификация UI секций между tier 1 и tier 2+.** Извлёк `_buildSectionsList(list)` — единый рендерер списка секций, используется и для основного состава рецепта, и для каждого дополнительного яруса.
- **Дополнительные ярусы (2+) теперь имеют все те же фишки**, что и ярус 1:
  - Drag-and-drop сортировка секций (иконка ⋮⋮ в заголовке секции).
  - Drag-and-drop сортировка ингредиентов внутри секции.
  - Autocomplete по истории при вводе названия ингредиента.
  - Per-section заметки.
  - Полноценный градиентный заголовок секции.
- Удалён `_buildSimpleSectionCard` (упрощённый рендерер для tier 2+ из v2.0.0). Теперь весь UI единообразный.

## v2.0.0 — 2026-04-24

🎂 **Multi-tier торты!** Теперь рецепт может состоять из нескольких ярусов разного размера — для свадебных тортов, ярусных, фигурных конструкций.

- **Модель:** Recipe получил поле `additionalTiers: List<TierData>`. Tier 1 живёт в существующих root-полях рецепта (`diameter`/`height`/`sections`), tier 2+ — в `additionalTiers`. Если поле пустое — рецепт одноярусный, поведение приложения идентично прошлым версиям.
- **Storage / Import-export:** дополнительные ярусы корректно сериализуются. Старые JSON без поля `additionalTiers` грузятся как одноярусные. Сохранённый JSON одноярусного рецепта НЕ содержит поле `additionalTiers` — экономим место и не пугаем старые версии клиента.
- **add_recipe форма:** под основным составом — кнопка «Добавить ещё один ярус». Каждый дополнительный ярус: метка («Низ»/«Верх»), диаметр, высота, свой состав. Можно удалить.
- **scaler_screen:** для multi-tier рецептов — отдельный экран. Каждый ярус показан как отдельная карточка со своим слайдером диаметра. Каждый ярус масштабируется **независимо** (вариант B из обсуждения). Сверху — общая инфа («N ярусов • итого ~X кг»). Список покупок и шаринг агрегируют ингредиенты со всех ярусов.
- **Дублирование рецепта** теперь корректно копирует и tier 1, и additionalTiers (ранее терялись доп.ярусы при копировании).
- **+5 unit-тестов** на multi-tier storage: round-trip, миграция старого JSON, эконом-сериализация для одноярусных.

**Известные ограничения v2.0.0:**
- В multi-tier scaler нет режима «по весу» — вес единственное число для всего торта, не масштабируется per-tier естественно. Используй размерное масштабирование.
- В additional tiers (ярус 2+) при редактировании используется упрощённый UI секций: без drag-and-drop сортировки, без autocomplete ингредиентов, без per-section заметок. Tier 1 как был — со всеми фишками. Унификация в v2.1.0.

## v1.18.0 — 2026-04-24

- **Убран подзаголовок с версией** в шапке списка («v.X.Y • Калькулятор рецептов»). Версия осталась в Settings → О приложении.
- **Поиск по ингредиентам** — поле поиска теперь матчит и по названию рецепта, и по именам ингредиентов. Подсказка обновлена: «Поиск по названию или ингредиенту».
- **Bulk-выбор и удаление.** В попап-меню рецепта (⋮) → «Выбрать (массовое удаление)» включает selection mode. Шапка меняется на «Выбрано: N» с кнопками «закрыть» и «удалить». Тапы по карточкам toggle'ят выбор. Drag отключён в режиме выбора. Подтверждение перед удалением.
- **«NEW» badge** на рецептах младше 24 часов — маленький розовый чип у названия. Recipe.id хранит timestamp, по нему и считаем.
- **Защита от удаления используемого custom-типа.** Если кастомный тип секции используется в рецептах — Settings показывает диалог со списком (до 5 названий + «и ещё N») и предупреждает, что уже сохранённые секции продолжат работать, но новые добавить будет нельзя. Подтверждение чтобы реально удалить.

## v1.17.0 — 2026-04-24

- **Quick-диаметры настраиваются в Settings.** Текстовое поле, перечислите через запятую (например `15, 17, 20, 22, 25, 28`). Значения парсятся, фильтруются (1–60 см), невалидные дропаются. Применяется к чипам под слайдером диаметра в `scaler_screen`. Дефолт без настройки — `16, 18, 20, 22, 24, 26`.
- **Default режим пересчёта** — выбор в Settings: «По размеру» или «По весу». Применяется при открытии рецепта в `scaler_screen`. Если у рецепта нет веса, режим «По весу» игнорируется (открывается «По размеру»).
- **«Проверить обновление сейчас»** — кнопка в Settings → Обновления. Работает даже если auto-проверка выключена (временно её включает на одну операцию). Показывает SnackBar с результатом.
- **Сброс настроек до дефолтов** — отдельная кнопка в опасной зоне Settings. Чистит тему, сортировку, auto-update toggle, default-режим, quick-диаметры. Рецепты и кастомные типы не трогаются.
- **Haptic при появлении баннера обновления.** При успешной проверке обновлений — лёгкий тактильный сигнал, чтобы заметить даже если телефон лежит экраном вниз.

**Заметка по push-уведомлениям:** настоящий Firebase-push не реализуем без сервера и FCM-проекта. Для текущего сценария (пользователь сам открывает приложение) добавили haptic + ручную проверку — этого достаточно.

## v1.16.0 — 2026-04-24

- **Экран настроек** через шестерёнку в шапке (вместо ⋮). Туда переехали:
  - **Тема** — Авто / Светлая / Тёмная (вместо отдельной иконки в шапке).
  - **Кастомные типы секций** — список с edit/delete inline + кнопка «Добавить тип» (раньше управление было только через long-press внутри пикера).
  - **Обновления** — переключатель «Автоматически проверять обновления» (на off `UpdateService` молчит).
  - **Резервные копии** — Экспортировать в JSON, Импортировать из JSON, Откатить последний импорт (виден только пока snapshot есть).
  - **Статистика** — открывает тот же bottom sheet (счёт рецептов, топ ингредиентов, топ тегов).
  - **Опасная зона** — «Удалить все рецепты и кастомные типы» с подтверждением (чистит recipes / recipes_backup / pre_import_backup / custom_section_types).
  - **О приложении** — версия, кликабельная ссылка на GitHub-репо, лицензия (MIT).
- **Сортировка** осталась в шапке отдельной иконкой `↕` (это про текущий вид списка, не глобальная настройка).
- **Декоративный фон формы рецепта** — два мягких розово-оранжевых пятна в противоположных углах за контентом, чтобы форма не казалась пустой между полями.

## v1.15.1 — 2026-04-24

- **Лочим приложение в портретной ориентации.** В landscape Android показывал системную подсказку «повернуть устройство», которая садилась прямо на FAB «Рецепт» в правом нижнем углу. UI и так не задизайнен под landscape, поэтому теперь приложение всегда в portrait — системная кнопка не появляется. `screenOrientation="portrait"` в манифесте + `SystemChrome.setPreferredOrientations` в `main()`.

## v1.15.0 — 2026-04-24

- **Запятая как десятичный разделитель в отображении.** `1.5 г` → `1,5 г`, `1.7 кг` → `1,7 кг`. Введён `formatNumber` / `formatGrams` в `lib/utils.dart`, применён в scaler, share-тексте, slider'ах, badge'ах. RU-стиль во всём UI.
- **Безопасный импорт.** Перед каждым импортом сохраняется snapshot текущей коллекции в `pre_import_backup`. В меню «⋮» появляется пункт «Откатить последний импорт» (виден только пока snapshot есть). Откат восстанавливает рецепты ровно как было до импорта.
- **Экран статистики** через меню «⋮» → «Статистика»: счётчик рецептов, всего ингредиентов, сумма заявленных весов рецептов, топ-5 ингредиентов по частоте использования, топ-5 тегов.

## v1.14.0 — 2026-04-24

- **Теги на карточках рецептов.** Под строкой «⌀ X см • N секц. • M ингр.» теперь видны все теги рецепта компактными чипами.
- **Сортировка списка рецептов.** В меню «⋮» появилась группа «СОРТИРОВКА» с вариантами: Вручную (по умолчанию — порядок drag&drop), Новые сначала, Старые сначала, По алфавиту. Выбранный режим сохраняется в SharedPreferences. Drag-to-reorder работает только в режиме «Вручную».
- **Управление кастомными типами секций.** В пикере секций long-press на кастомном типе открывает диалог с действиями «Изменить» (открывает форму редактирования с заполненными полями) и «Удалить». Маленькая иконка ✏️ отличает кастомный тип от пресета.
- **Обработка ошибок камеры/галереи.** При неудачном `image_picker` (например, отказ в системном диалоге камеры) показывается понятный SnackBar вместо немой ошибки.
- **Лучший empty state при фильтрации.** Когда поиск или фильтр-тег не дают результатов — показывается описание контекста («нет рецептов по запросу «X» с тегом «Y») и кнопка «Сбросить фильтры», которая разом чистит и поиск, и тег.

## v1.13.0 — 2026-04-24

- **Hero-фото в шапке экрана пересчёта.** Если у рецепта есть фото, оранжевый градиент в верхней части `scaler_screen` заменяется самой картинкой с тёмным overlay (45%), чтобы белые иконки и текст оставались читаемыми.
- **Drag-and-drop секций** в форме рецепта — у каждой секции в заголовке появилась иконка ⋮⋮, long-press которой запускает перетаскивание.
- **Drag-and-drop ингредиентов** внутри секции — та же иконка слева от поля «Ингредиент». Можно переставлять порядок ингредиентов внутри одной секции.
- **`RadioGroup` миграция.** Переписал диалог «Свой тип секции» с устаревшего `RadioListTile.groupValue/onChanged` на современный `RadioGroup` ancestor (Flutter 3.32+ API). Шесть info-предупреждений в analyze исчезли.

## v1.12.2 — 2026-04-23

- **Hotfix:** заменил `file_picker` на `file_selector` (официальный Flutter-плагин), чтобы убрать неразрешимый конфликт `win32` версий между `file_picker`, `share_plus` и `package_info_plus`. Импорт работает идентично — open dialog с фильтром по `.json`.

## v1.12.1 — 2026-04-23 (release не опубликован, см. v1.12.2)

- Hotfix `file_picker` namespace, который сломал win32-зависимости.

## v1.12.0 — 2026-04-23 (release не опубликован, см. v1.12.2)

- **Теги/категории.** В рецепте — поле тегов через запятую (например, «шоколадный, без глютена»). В списке появляется лента-фильтр по тегам, всегда есть кнопка «Все» для сброса.
- **Кастомные типы секций.** В пикере секций — кнопка «+ Свой тип»: вводишь название, эмодзи, выбираешь режим масштаба (объём/площадь/фикс). Сохраняется отдельно от пресетов.
- **Экспорт / импорт коллекции.** В меню «⋮» в шапке: «Экспортировать» собирает JSON всех рецептов и кидает в системный share-диалог; «Импортировать» открывает file-picker, читает JSON, добавляет к существующим. Картинки в JSON не передаются (пути локальные).
- **Drag & drop пересортировки рецептов.** Long-press на карточку → перетаскивание. Работает только когда фильтры (поиск/тег) выключены — иначе сортировка по видимому списку не отражается на хранилище. Drag для секций и ингредиентов внутри рецепта — отложен (нужны nested scrollable аккуратные).
- **Автокомплит ингредиентов.** Поле имени ингредиента превратилось в `Autocomplete` с подсказками из истории — все когда-либо введённые названия отсортированы по частоте.
- **Фото/обложка рецепта.** В форме сверху — круглый аватар, тап открывает выбор «Из галереи / Сделать фото / Убрать». Картинка копируется в `applicationDocuments` (переживает рестарты). В списке миниатюра заменяет дефолтный 🍰. На экране пересчёта пока без hero-фото — будет в следующем релизе.

## v1.11.0 — 2026-04-23

- **Дублирование рецепта.** В меню «⋮» рядом с рецептом появилась команда «Дублировать» — создаёт копию `{title} (копия)` со всем содержимым.
- **Undo при удалении.** Вместо подтверждающего диалога — прямое удаление со SnackBar «Отменить» (4 секунды). Меньше тапов, но безопасно.
- **Меню действий вместо двух иконок.** Edit + Delete заменены на компактную «⋮» с пунктами Редактировать / Дублировать / Удалить.
- **Quick-buttons частых диаметров** под слайдером пересчёта по размеру: 16/18/20/22/24/26 см. Тап — сразу выставляет диаметр (с haptic feedback).
- **Заметки на уровне секции.** В форме рецепта у каждой секции своё поле «Заметка к секции» (например, «замесить 5 минут на средней»). На экране пересчёта показывается курсивом под ингредиентами.
- **Тёмная тема** + переключатель Авто / Светлая / Тёмная (иконка в шапке списка). Предпочтение сохраняется в SharedPreferences. Базовая инфраструктура; некоторые хардкодные цвета в редких местах могут выглядеть не идеально — будут отполированы по мере находок.

## v1.10.4 — 2026-04-23

- **Hotfix:** в v1.10.3 R8 минификация ломала release-билд из-за `Missing class com.google.android.play.core.*` — Flutter ссылается на Play Core (deferred components), которого нет как зависимости. Добавлены `-dontwarn` правила в `android/app/proguard-rules.pro`. APK снова собирается.
- Содержит **все изменения v1.10.3** (тег v1.10.3 в гите есть, но release с APK не был опубликован — используйте v1.10.4).

## v1.10.3 — 2026-04-23 (release не опубликован, см. v1.10.4)

- **`versionCode` теперь монотонно растущий** (unix timestamp). Новый APK v1.10.3+ корректно ставится поверх старой версии без конфликта пакета.
- **Баннер обновления больше не показывается на iOS/desktop/web** — там APK install не работает, баннер был бесполезен.
- **`_isNewer` стал устойчив к non-strict semver** — версии вроде `1.10.2-beta` теперь сравниваются по числовой части каждого сегмента, не падают.
- **Валидация рецепта при сохранении** — пустые ингредиенты (без имени или с весом ≤ 0) и пустые секции автоматически отбрасываются. Если после очистки рецепт пустой — ошибка с понятным текстом.
- **Запятая как десятичный разделитель** — теперь можно вводить `1,5` (привычно для RU-локали), не только `1.5`. Действует на размеры формы, вес и количество ингредиентов.
- **ProGuard / R8 включены** для release-билдов (`isMinifyEnabled = true`, `isShrinkResources = true`). Должно заметно уменьшить размер APK.
- **Pre-commit hook** в `.githooks/pre-commit` — гоняет `flutter analyze` локально перед коммитом. Включается командой `git config core.hooksPath .githooks` (см. README).
- **Лицензия:** добавлен `LICENSE` (MIT).
- **+12 тестов:** 6 на `aggregateIngredients` (case-insensitivity, дедупликация, пустые имена), 6 на `StorageService` (миграция старого JSON без `notes`, backup/restore при битом main JSON, защита от полной потери данных). Всего 21 тест.

## v1.10.2 — 2026-04-23

- **Гладкая установка обновления.** `UpdateService` теперь явно запрашивает разрешение «Установка из неизвестных источников» через `permission_handler` ДО открытия APK. Раньше Android отбрасывал в Settings и не возвращал к диалогу установки. После однократного предоставления разрешения следующие апдейты проходят одним тапом без захода в настройки.
- **Осмысленные ошибки обновления.** Введён `UpdateException` с понятным текстом (например, «Нужно разрешить установку из неизвестных источников в настройках Android.»). Раньше любая ошибка показывала «Ошибка скачивания. Проверьте интернет.» — даже когда дело было не в интернете.

## v1.10.1 — 2026-04-23

- **CI** — новый workflow на push в `main` и pull request: прогоняет `flutter analyze` + `flutter test`.
- **Release workflow** обновлён до actions без Node 20 deprecation: `actions/checkout@v6`, `actions/setup-java@v5`, `softprops/action-gh-release@v3`.

## v1.10.0 — 2026-04-23

- **Несколько секций одного типа** в рецепте (например, два разных крема). Раньше второй крем добавить было нельзя.
- **Заметки / шаги приготовления** — новое поле `notes` у рецепта, отображается над списком ингредиентов на экране пересчёта.
- **Поиск рецептов** по названию — появляется в шапке списка от 5 рецептов и более.
- **Список покупок** — bottom sheet с агрегированными ингредиентами (одинаковые названия суммируются по всем секциям).
- **Поделиться рецептом** — формирует текст с размерами, секциями и заметками; открывает системный share-диалог.
- **Бэкап рецептов** — `StorageService` сохраняет предыдущий валидный JSON, при битом основном — поднимает резервную копию вместо потери всех рецептов.
- **Расширены диапазоны слайдеров пересчёта**: диаметр 10–50 см (было 10–35), вес 100 г – 20 кг (было 100 г – 10 кг). Совпадает с диапазоном ручного ввода.
- **Чистка кода**: удалено мёртвое поле `_myVersion` в `main.dart` и неиспользуемый импорт.
- **README** переписан, **CHANGELOG.md** заведён.

## v1.9.0 — 2026-04-23

- **Фикс расчёта по весу с fixed-секциями.** До этого итоговый вес рецепта не сходился с целевым на сумму fixed-секций (например, декор). Теперь fixed-вес исключается из обоих весов перед расчётом коэффициента.
- **Бейдж итогового веса** на экране пересчёта — всегда показывает «итого ≈ X г / X.X кг».
- **Юнит-тесты** на `RecipeScaler` (9 шт), включая отдельный тест на починенный баг.
- Сломанный дефолтный `widget_test.dart` (ссылался на несуществующий `MyApp`) удалён.
- **`release.ps1` → `release.sh`** — сборка переехала с Windows на Linux.

## v1.8.0 — 2026-04-14

- Доработка баннера обновления.

## v1.7.0 — 2026-04-14

- Отображение текущей версии приложения, сохранение рецептов.

## v1.6.0 — 2026-04-14

- Локальное сохранение рецептов через `SharedPreferences`.

## v1.5.0 — 2026-04-13

- Багфиксы.

## v1.4.0 — 2026-04-13

- Auto-download и установка обновлений из GitHub Releases (Android APK).
- v1.4.1 / v1.4.2 — фикс подписанного релизного билда.

## v1.3.0 — 2026-04-13

- Описание изменений в баннере обновления.

## v1.2.0 — 2026-04-09

- Фикс баннера обновления, ручной ввод размера и веса.

## v1.1.0 — 2026-04-09

- Ручной ввод значений.

## v1.0.0 — 2026-04-09

- Первый релиз: рецепты с секциями, ручной пересчёт по диаметру/высоте.
